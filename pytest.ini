[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
# pytest-socket blocks ProactorEventLoop self-pipe creation on Windows — disabled here
# since all network calls are mocked in tests
addopts = -p no:socket
