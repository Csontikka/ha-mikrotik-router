[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
wheel = "*"
pygithub = ">=2.0"
homeassistant = ">=2024.3.0"
pre-commit = ">=3.0"
pylint = ">=3.0"
pytest-aiohttp = ">=1.0"
pytest-cov = ">=4.0"
pytest = ">=7.0"

[packages]
librouteros = ">=3.4.1,<4.0.0"
mac-vendor-lookup = ">=0.1.12"
